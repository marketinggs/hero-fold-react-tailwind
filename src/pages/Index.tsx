import HeroSection from "@/components/HeroSection";

const Index = () => {
  return (
    <div className="min-h-screen">
      <HeroSection />
      
      {/* Additional sections will be added in future updates */}
      {/* We'll only implement the first fold for now */}
    </div>
  );
};

export default Index;
